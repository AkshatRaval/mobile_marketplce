export const sendPushNotification = async (expoPushToken: string, title: string, body: string, data: any = {}) => {
    // Mock implementation to prevent crash
    console.log("Push notification suppressed (Mock Mode)");
};
